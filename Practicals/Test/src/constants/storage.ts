// Key used to store and retrieve the contacts list from AsyncStorage.
// Keeping this in a single constant avoids typos when accessing storage
// from multiple places in the app.
export const CONTACTS_STORAGE_KEY = "phone-contact-manager-contacts";